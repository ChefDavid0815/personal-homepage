# CHROMA / Signal Remix publication

2026-09-22 · Website 1.22.0 · CHROMA 0.1.0

## Delivered / 交付

- First of six personal Gallery projects: CHROMA, followed by ProjectLens, Festival Toolkit, AXIOM, Folio and NBA After Hours. School and modeling collections remain separate.
- Original Y2K instrument installation using the application's blue, yellow, red, cyan, chrome and fonts. CPU / GPU / RAM feature perspectives, moving traces, floating monitor, burst stars, raised keys, metal light and pixel tiles.
- Three actual interface views: Simple, PRO and Mini. PRO switches between actual Chinese and English captures. Other screenshots explicitly retain their actual capture language. Decorative traces are artwork, not visitor hardware readings.
- Bilingual Now entry 015 with a matching miniature; ninth bilingual Post with four chapters and real screenshots. Website and GitHub profile documentation updated in both languages.
- Fixed the existing Gallery grid's oversized horizontal gaps at tablet widths while preserving its vertical rhythm and individual project identities.

主展厅新增第一件作品 CHROMA，保留其他项目各自的设计语言。新增第十五个 Now 节点与第九篇双语随笔，GitHub 个人主页和软件文档同步。电光蓝、亮黄、信号红、青色、金属仪器与字体均沿用软件风格。展柜不读取访问者的硬件。

## Checks / 检查

- CHROMA calculation tests: **13/13 passed**. TypeScript and Vite production build passed.
- A fresh, read-only native collector smoke check completed three samples: connected, 190 sensor records, CPU utilization and temperature available, two GPU records, enhanced CPU service connected. The check reused the existing local installation; no drivers were installed and no hardware settings were changed.
- GitHub `v0.1.0` source tag and early-access release published. SHA-256 digests of all five uploaded assets match the corresponding local files: setup, portable, source archive, checksums and verification report.
- Website JavaScript checks and shared motion-state test passed.
- Browser: Chinese/English Gallery copy; CPU/GPU/RAM controls; Simple/PRO/Mini capture selection; selected channel and capture retained after language change. English article uses its actual English PRO screenshot.
- Browser: detail dialog, Mini selector, Escape close and focus return to the opener. Global motion control pauses the instrument animations.
- Responsive review at 1440 x 1000, 830 x 1000 and 390 x 844. Desktop Gallery and Now, bilingual article, mobile Gallery/dialog/article/Now inspected. Tablet Gallery card and grid widths match after the gap fix. Mobile Gallery, Now and article document widths match the available viewport.
- Six Gallery exhibits, fifteen Now entries and nine chronological Posts rendered. The newest Post has four chapters; previous articles and dated entries remain intact.

本次重新运行了软件基础计算测试、生产构建及只读本机采集检查；网站检查涵盖新展柜交互、双语状态、手机和平板布局、详情弹窗及全站动效开关。原始采集报告、进程列表和完整 QA 目录没有上传。

## Boundaries / 范围

This publication pass does not re-run every native desktop UI flow, installer path, long-duration load test or in-game FPS scenario. Selected interface images come from the application's existing QA captures; the app's own verification report distinguishes that evidence. The binaries are unsigned early access. Hardware support varies. The mini window is a desktop overlay, not an injected game OSD. CHROMA requires Windows for real collection; no browser-only monitor is deployed.

本次发布检查不等于重新完成全部安装流程、游戏内帧率验证、长时间压力或其他硬件实测。真实界面图片来自软件已有 QA 截图，测试边界见软件仓库。0.1.0 为未签名早期版本；小窗属于桌面置顶窗口，真实采集需要 Windows 应用。
